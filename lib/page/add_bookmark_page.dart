import 'package:flutter/material.dart';

class AddBookmarksPage extends StatelessWidget {
  const AddBookmarksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Add Bookmarks"),
      ),
      //body: ,
    );
  }
}
